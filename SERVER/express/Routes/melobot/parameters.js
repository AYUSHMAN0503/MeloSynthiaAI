// Define a constant for the chatbot parameters.
const melobotChatParams = [
  {
    name: 'message',
    description: 'Message to send to Melobot',
  },
];

// Export the chatbot parameters as an object.
module.exports = {
  melobotChatParams,
};
